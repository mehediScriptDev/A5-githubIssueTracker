
1️⃣ What is the difference between var, let, and const?
-Unlike var which is function-scoped, let and const are block-scoped, with let allowing its value to be reassigned while const does not.
2️⃣ What is the spread operator (...)?
-The spread operator (...) unpacks elements from an iterable,
3️⃣ What is the difference between map(), filter(), and forEach()?
-map() creates a new array by transforming every element, filter() creates a new array keeping only elements that pass a condition, and forEach() simply executes a function on each element without returning a new array
4️⃣ What is an arrow function?
-An arrow function is a compact, ES6 JavaScript function expression that uses the => syntax
5️⃣ What are template literals?
-emplate literals are JavaScript strings enclosed in backticks (`) that allow for multi-line text and easy string interpolation using embedded expressions (${})